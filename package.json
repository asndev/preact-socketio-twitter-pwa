{
  "name": "preact-twitter-stream-pwa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "start-server": "node server.js",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "test": "eslint src && preact test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^6.6.0",
    "if-env": "^1.0.0",
    "preact-cli": "^1.0.1",
    "prettier": "^1.3.1"
  },
  "dependencies": {
    "express": "^4.15.3",
    "http": "0.0.0",
    "preact": "^8.1.0",
    "preact-router": "^2.5.2",
    "socket.io": "^2.0.1",
    "twitter": "^1.7.0"
  }
}
